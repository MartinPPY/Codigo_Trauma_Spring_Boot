version: '3.8'
services:

#SERVICIO PARA LEVANTAR UNA BD POSTGRES LOCALMENTE

#  postgres:
#    image: postgres:18  # usa una versión fija, evita "latest"
#    restart: always
#    environment:
#      POSTGRES_USER: ${POSTGRES_USER}
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#      POSTGRES_DB: ${POSTGRES_DB}
#    ports:
#      - "5432:5432"
#    volumes:
#      - ./postgres:/var/lib/postgresql
#    
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
      
    restart: always
